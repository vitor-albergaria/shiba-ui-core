import { t } from 'i18next';
import { Meta, Typeset } from '@storybook/blocks';
import { fontSize } from '../../../../src/theme/constants/fontSize';
import { DocsTitle, DocsDescription } from '../../components/TextElements';

<Meta title="Quick Start/Styling" />

<DocsTitle text="docs_styling_title" />
<DocsDescription text="docs_styling_description_one" />

```bash
npm install styled-components
```

```bash
yarn add styled-components
```

<DocsDescription text="docs_styling_description_two" />

```jsx
import styled, { useTheme } from 'styled-components';

const StyledButton = styled.button`
  background-color: ${(props) => props.theme.color.error};
  color: ${(props) => props.theme.color.text};
`;

const MyButton = () => {
  const theme = useTheme();

  return <StyledButton theme={theme}>Custom Button</StyledButton>;
};
```

```jsx
import { MyButton } from './MyButton';

const App = () => {
  return (
    <div>
      <MyButton />
    </div>
  );
};
```
